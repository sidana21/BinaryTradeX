# متغيرات البيئة المطلوبة للتطبيق

# ═══════════════════════════════════════════════════════════════
# 🔐 متغيرات الأمان (إلزامية في الإنتاج)
# ═══════════════════════════════════════════════════════════════

# مفتاح سري للجلسات - يجب تغييره في الإنتاج
# قم بتوليد مفتاح عشوائي قوي (32 حرف على الأقل)
# مثال: يمكنك استخدام: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# ═══════════════════════════════════════════════════════════════
# 🌍 إعدادات البيئة
# ═══════════════════════════════════════════════════════════════

# بيئة التشغيل: development أو production
# على Render يجب أن تكون: production
NODE_ENV=production

# ═══════════════════════════════════════════════════════════════
# 🔌 إعدادات المنفذ
# ═══════════════════════════════════════════════════════════════

# المنفذ الذي سيعمل عليه التطبيق
# على Replit: يجب أن يكون 5000
# على Render: سيتم تعيينه تلقائياً بواسطة Render (عادة 10000)
# لا تحتاج لتغييره - سيستخدم القيمة التلقائية المناسبة
PORT=5000

# ═══════════════════════════════════════════════════════════════
# 📊 إعدادات Binomo (اختيارية)
# ═══════════════════════════════════════════════════════════════

# إذا كنت تريد الاتصال بـ Binomo الحقيقي (غير مطلوب للتداول التجريبي)
# BINOMO_AUTHTOKEN=your-binomo-auth-token-here
# BINOMO_DEVICE_ID=your-device-id-here

# ═══════════════════════════════════════════════════════════════
# 💾 قاعدة البيانات (إذا كنت تستخدم PostgreSQL)
# ═══════════════════════════════════════════════════════════════

# على Render، سيتم تعيينه تلقائياً إذا أضفت PostgreSQL database
# DATABASE_URL=postgresql://user:password@host:port/database

# ═══════════════════════════════════════════════════════════════
# 📝 ملاحظات مهمة
# ═══════════════════════════════════════════════════════════════

# 1. لا تشارك ملف .env الحقيقي أبداً - هذا مثال فقط
# 2. على Render: أضف المتغيرات في Environment Variables في لوحة التحكم
# 3. على Replit: أضف المتغيرات في Secrets (القفل في الشريط الجانبي)
# 4. SESSION_SECRET يجب أن يكون مختلفاً وعشوائياً في الإنتاج
